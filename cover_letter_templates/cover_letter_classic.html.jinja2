<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Cover Letter</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: {{ font_size }};
            color: {{ colors.primary }};
            background-color: {{ colors.background }};
            margin: 40px;
            line-height: 1.6;
        }
        .header {
            border-bottom: 2px solid {{ colors.highlight }};
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        .contact-info {
            margin-bottom: 20px;
        }
        .recipient-info {
            margin-bottom: 20px;
        }
        .content {
            margin-bottom: 20px;
        }
        .closing {
            margin-top: 20px;
        }
        ul {
            padding-left: 20px;
        }
        li {
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>{{ name }}</h1>
        <div class="contact-info">
            <p>{{ email }} | {{ phone }}{% if city %} | {{ city }}{% endif %}</p>
        </div>
    </div>
    <div class="recipient-info">
        <p>{{ recipient_name | default('Hiring Manager') }}<br>
        {{ company_name }}<br>
        {{ city | default('') }}</p>
    </div>
    <p><strong>Objet :</strong> Candidature pour {{ job_title }}</p>
    <div class="content">
        <p>Cher(e) {{ recipient_name | default('Hiring Manager') }},</p>
        <p>Je suis enthousiaste à l'idée de postuler pour le poste de {{ job_title }} chez {{ company_name }}. Voici pourquoi je suis un excellent candidat :</p>
        {{ motivation_points | safe }}
        <p>{{ closing_statement | default('Je vous remercie pour l\'attention portée à ma candidature et reste disponible pour un entretien.') }}</p>
    </div>
    <div class="closing">
        <p>Cordialement,<br>{{ name }}</p>
    </div>
</body>
</html>
